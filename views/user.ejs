<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    body {
      background-color: lightblue;
    }
  </style>
  <title>User List App </title>
</head> 

<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">User List App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
            <a class="nav-link" href="/addUser">Add new user</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/about">About</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <br>
    <div class="text-center">
      <h2>User Details</h2>
    </div>
    <br>
    <% if (user) { %> 
      <div>
        <p><b>User Name:</b><br>
          <%= user.user %>
        </p>
        <p><b>User Image:</b><br> <img src="<%= user.image %>" width="20%"></p>
        <p><b>Date of Birth:</b><br>
          <%= new Date(user.dob).toLocaleDateString('en-US', { year: 'numeric' , month: 'short' , day: 'numeric' })
            %>
        </p>
        <p><b>Weight:</b><br>
          <%= user.weight %>
        </p>
        <p><b>Blood Pressure:</b><br>
          <%= user.bloodpressure %>
        </p>
        <a href="/" class="btn btn-secondary">Back</a>
        <a href="/editUser/<%= user.recordID %>" class="btn btn-primary" style="background-color: rgb(45, 45, 133); border-color: lightblue;">Edit</a>
      </div>
      <% } else { %>
        <p>No user found.</p>
        <% } %>
  </div>

</body>

</html>
